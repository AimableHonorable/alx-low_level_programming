#include "holberton.h"

/**
 * _isdigit - function to check digit from 0 to 9
 * @c: int to be checked
 * Return: 1 if it is a digit and 0 if not
 */
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
