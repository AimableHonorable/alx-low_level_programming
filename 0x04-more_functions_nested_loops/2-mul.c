#include "holberton.h"

/**
 * mul - a function to multiply two integers
 * @a: first integer
 * @b: second integer
 * Return: should return 0 when true
 */
int mul(int a, int b)
{
	return (a * b);
}
